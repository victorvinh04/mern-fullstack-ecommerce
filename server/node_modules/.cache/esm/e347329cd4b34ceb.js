let express,mongoose,cors,readdirSync,morgan;_d4b‍.w("express",[["default",["express"],function(v){express=v}]]);_d4b‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_d4b‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_d4b‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_d4b‍.w("morgan",[["default",["morgan"],function(v){morgan=v}]]);





require("dotenv").config();

const app = express();

const corsOptions = {
  origin: ["http://localhost:3000"],
  optionsSuccessStatus: 200,
};

// db connecting

mongoose
  .connect(process.env.DATABASE_URL)
  .then(() => {
    console.log("DB connected");
  })
  .catch((err) => _d4b‍.g.console.error("DB CONNECTION ERROR =>", err));

app.use(express.json({ limit: "5mb" }));
app.use(express.urlencoded({ extended: true }));
app.use(
  cors({
    origin: ["http://localhost:3000"],
  })
);

readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

app.get("/", (req, res) => {
  res.send("Hello, world!");
});

const port = process.env.PORT || 5001;

app.listen(port, () => console.log(`Server is running on port ${port}`));
